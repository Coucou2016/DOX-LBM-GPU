# Runtime dependencies (Windows / Python 3.13 verified).
# Install MS model only — avoid full finitewave (numpy/MSVC conflicts on Windows):
#   pip install -r requirements.txt
finitewave-model-mitchell-schaeffer==0.6.0
numpy>=1.26,<3
# Optional for publication figures (scripts/plot_science.py):
#   pip install SciencePlots
# SciencePlots>=2.1.0
